# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
*.tfvars
!example.tfvars

# Compiled Ignition (contains SSH keys / tokens once you fill placeholders)
butane/*.ign

# Worker join tokens
worker-join-token.txt

# Kubeconfig (contains cluster credentials)
*kubeconfig*.yaml

# OS/editor noise
.DS_Store
.idea/
.vscode/

LEGACY.md
!README.md